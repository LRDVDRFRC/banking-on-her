# IFC Sprint Platform — Build Backlog

Work queue for unattended runs. See `ROADMAP.md` for why: the toolkit must be sprint-ready in 24 hours; the platform waits for revenue.

Rules for any agent working from this file:

- Work **top-to-bottom through the "Sprint-critical" section**. Complete and verify each item fully before starting the next; check items off as you go — do not stop after one item while unchecked sprint-critical items remain.
- Do **NOT** build anything under "Platform — deferred" — a human will move items up when the first paid sprint justifies them.
- When done with an item: check it off, append `(done YYYY-MM-DD — one-line summary)`.
- Verify your work before checking off (open HTML via a local server and check the console, run scripts and confirm output).
- Everything stays **local** — no deploys, no pushes, no emails, nothing outward-facing.
- All HTML output follows the Prism brand idiom of `03_proposition-deck.html` (Sora/Inter, glass panels, palette: ink #0D3B2E, sky #6DC0C8, rose #F5B896, amber #F2D080, mint #9FD4B0). The brand mark is the `.prism-mark` SVG in that file (3D glass prism, spectrum beams fading out) — reuse it verbatim; never use a bare line-into-stripes divider.
- Readiness scores are ALWAYS percentages (0–100%) rendered as filling rings (`.ring` component in `03_proposition-deck.html`) — never A–D letter grades.
- Client-facing copy in Dutch; internal/facilitator material in English.
- If blocked, write a `> BLOCKED:` note under the item, log it, and continue with the NEXT sprint-critical item (don't let one blocker stall the whole run).
- Log each item at the bottom under **Run log**.

## Sprint-critical — build ALL of these (in order)

- [x] **Sales one-pager** — `04_sales-onepager.html`: a CFO-facing page that sells the 24-hour sprint to BeFrank. Structure: the gap (NL gender pension gap, 36–40%, €400/mo) → the promise (one day in, board-ready proposition out) → how it works (7-phase arc from README.md) → what you walk out with → investment + the ask (book the sprint). Tone: fact-based, commercial, zero DEI-fluff. (done 2026-06-10 — built in-session, verified in browser/CLI)
- [x] **Self-assessment as fillable form** — `05_self-assessment-form.html`: convert `02_Self_Assessment.md` into an interactive HTML form: 29 statements, 0–4 sliders/radios per question, live per-dimension readiness percentages shown as **filling rings**, overall score ring, and an "export answers as JSON" button (download). Works offline, single file. (done 2026-06-10 — built in-session, verified in browser/CLI)
- [x] **Intake pack** — `06_intake-pack.html`: client-facing pre-sprint page (Dutch): what the sprint is, the data-room request list (from run-of-show §2), the self-assessment link, the shared pre-work question, and the T–7 → T–1 timeline. (done 2026-06-10 — built in-session, verified in browser/CLI)
- [x] **Diagnostic Engine v1 (offline)** — `07_diagnostic-engine.html`: single-file tool that ingests one or more self-assessment JSON exports (file input), merges them, computes per-dimension readiness percentages, shows divergence between respondents per dimension, and renders the same 5-ring layout as deck slide 3. Include a "copy scores into the deck" output block. (done 2026-06-10 — built in-session, verified in browser/CLI)
- [x] **Build-brief template** — `BRIEF_TEMPLATE.md` + `sample-build-brief.json`: the structured brief the Day-1 ideation session fills at 17:00 (mandate, success metrics, chosen proposition, persona, journey moment, artifacts wanted, tone, hard constraints) so the overnight AI build is executable. Markdown for the room, JSON schema for the machine. (done 2026-06-10 — built in-session, verified in browser/CLI)
- [x] **Presentation Builder v1** — script `08_fill-deck.js` (Node): reads a JSON file of sprint data (readiness percentages, mandate, metrics, business-case numbers) and produces a filled copy of `03_proposition-deck.html` with all `«tokens»` and ring `--p` values replaced. Include `sample-sprint-data.json` and verify the output renders. (done 2026-06-10 — built in-session, verified in browser/CLI)
- [x] **End-to-end demo dry run** — wire it together with demo data: sample assessment JSONs → diagnostic engine scores → `08_fill-deck.js` → filled deck, and write `DEMO.md` describing the click-path for a full simulated sprint. (done 2026-06-10 — built in-session, verified in browser/CLI)

## Platform — authorized by user 2026-06-10 ("ok lets go and build this")

- [x] **Phase 1 MVP scaffold** — new repo folder `~/IFC/platform/`: Next.js (App Router) + libSQL/Turso schema for `sprints`, `participants`, `assessments`, `scores`. Pages: intake form, assessment form, facilitator dashboard. Local SQLite file for dev (no Turso credentials needed). README with run instructions. Do not deploy. Must stay data-compatible with the offline toolkit: assessment JSON schema = the 05-form schema; dashboard exports sprint-data JSON consumable by `08_fill-deck.js`. (done 2026-06-10 — Next.js 15 + libSQL, seeded demo, build clean, all 5 routes 200, export verified against 08_fill-deck.js)

## Run log

_(runs append here: date — item — result)_

- 2026-06-12 — SPRINT-DAY ENGINE (Phase 4→7) — live in prod, full chain verified on demo: /dashboard/[id]/canvas = moment × mechanism ideation grid (sector-aware Dutch axes, AI-suggest seeds 6-8 diverse concepts from the findings, chosen-counter + diversity warnings); /dashboard/[id]/concepts/[id] + build route = AI generates valueprop + clickable phone-frame mock + Dutch test script per chosen concept (~45s); /s/[token]/test/[id] = tablet test view with FeedbackCapture (panelist chips, 3×1-5 scores, quotes, auto-advance); /api/feedback token-gated; /dashboard/[id]/readout + generate = AI synthesis of all panel feedback → ranked concepts, scores, resonated/fell-flat, pilot recommendation. Demo chain in prod: 8 AI concepts, 3 chosen, 1 prototype built, 3 panelist reactions, readout ranks "Inleg blijft staan, tenzij je kiest" #1. Also this wave: FacilitatorNav header (hidden on client pages), AI-composed Dutch pre-read (preread_json + trigger), research-brief perf fix (web_search_20250305 — the 20260209 dynamic-filtering variant ran 410s vs 50s).

- 2026-06-11 — SPRINT v2: AI intake layer + phased methodology — (1) AI intake interview: /s/<token>/interview, Dutch conversational deep-dive (≤6 questions, builds on pre-work answer), stores transcript + EN summary + Dutch pull-quotes per participant; (2) Data room: client + facilitator uploads (pdf/pptx/docx/txt/csv ≤4MB), text extraction (unpdf/jszip), AI excerpt + key stats + relevance per doc; (3) Deep research: website field on sprint creation, one-click web-search brief (Company snapshot / Inclusive-finance angle / Competitors & best practices / Hooks / Sources) — FIXED perf bug: web_search_20260209's dynamic filtering blew runs to 410s, switched to web_search_20250305 → 50-65s, verified LIVE IN PRODUCTION (59s, brief in Turso); (4) /dashboard/[id]/findings: the Phase-1 "mirror" page assembling rings+divergence, crew voices, doc excerpts, research brief, library evidence, goal-of-the-day + ?preread=1 condensed teaser variant with print CSS; (5) Run-of-show v2: phased format (Mirror → Goal → Discovery → Ideate → AI build → evening test panel → next-morning readout), panel recruitment as critical T–7 checklist item, moment×mechanism diversity rule; README arc table updated. All deployed to production.

- 2026-06-10 (late) — Autonomous batch #15-17 complete + deployed — (1) /dashboard/[id]/deck: one-click Day-2 boardroom deck filled from live sprint data (deck template embedded in lib/deck-template.ts, TS port of the 08 filler in lib/fill-deck.ts; client name swapped for BeFrank, rings/totaaloordeel/datum filled, business «X» tokens left for real data); (2) /dashboard/[id]/briefing: deterministic facilitator prep — rings, sprint-focus dims, 20 band×dimension narratives with sector vocab, divergence flags with named extremes, pre-work voices, Dutch draft mandate, top-5 evidence with key stats, 11:00 reveal script; (3) BenchmarkPanel on every dashboard (this sprint vs anonymised portfolio baseline, n-counter — the Year-2 membership benchmark foundation) + participants can revise answers ("Pas je antwoorden aan", replace-not-duplicate verified). Dashboard now has 4 actions: deck / briefing / dossier / JSON export. All verified locally + deployed (unknown-id routes 404 correctly in prod).

- 2026-06-10 (evening) — Library 2.0 + AI layer — /research reorganised as a library (search, facets, topic shelves, detail pages, key-stat pills extracted from verified excerpts, copy-as-AI-context buttons); /api/library serves the evidence base as JSON or markdown digest; /dashboard/[id]/dossier generates a client-facing Dutch evidence dossier (Prism brand, print-ready); /research/ask = Claude-powered Q&A grounded in the stored reports with citations (claude-opus-4-8, adaptive thinking, digest cached as system block) — env-gated, activates when ANTHROPIC_API_KEY is set. All deployed to production and smoke-tested (research/ask/library/dossier routes 200, API basic-auth 401 unauth, 12-report digest live). Remaining from autonomous batch: dashboard deck generation (#15), diagnose briefing (#16), benchmark + resubmission (#17).

- 2026-06-10 — Sector/region awareness + research library — sprints now carry sector (pensioen/bank/verzekeraar/vermogensbeheer/hypotheek/algemeen) + region (nl/eu/global); the 29 statements are sector-adaptive via vocabulary tokens + 10 per-sector overrides (pensioen resolves byte-identical to the originals; schema/scoring unchanged). New /research library (basic-auth): filters, quick-read excerpts, add-report form, scripts/import-research.mjs. Seeded with 12 REAL verified reports (Netspar, ABP, CBS, Nibud, EC Pension Adequacy, EIOPA, OECD, McKinsey, BCG, Oliver Wyman, World Bank Findex, IFC/AXA SheForShield) — every URL fetched, every excerpt number checked against the source. Dashboards auto-surface matching reports by sector+region. Deployed to production; 12 reports live in Turso. Workflow for new assignments: tell Claude "new assignment: client/sector/region" → research agent extends research-seed.json → npm run import-research.

- 2026-06-10 — DEPLOYED to production (user authorized). https://ifc-sprint-platform.vercel.app — Vercel project ifc-sprint-platform, Turso DB ifc-sprint (aws-eu-west-1). Pre-deploy fixes: basic auth on / and /dashboard/* (middleware.ts; creds in platform/.env.production.local), pre-work question generalized to sprint client name. Smoke-tested live: 401 unauth / 200 auth / 404 unknown token / sprint creation writes to Turso (test row removed). Client links (/s/<token>/…) remain token-gated, no basic auth.

- 2026-06-10 — Platform MVP (~/IFC/platform) — user authorized same day; built by agent, verified: npm build clean, seed OK, / + intake + assessment + dashboard + export all HTTP 200, export JSON feeds 08_fill-deck.js (7 replacements applied), no-JS client flow works, browser pass confirmed home/dashboard/intake render on-brand with zero console errors. Caveats: no auth on dashboard (local-only acceptable), pre-work question hardcodes BeFrank, one submission per participant.
- 2026-06-10 — ALL 7 sprint-critical items — built in-session (5 parallel agents + main session), ahead of the scheduled nightly run. Verified: 04/05/06/07 load with zero console errors and correct behaviour (form export gating, engine demo-data merge + divergence, copy-scores block); 08_fill-deck.js ran on sample-sprint-data.json → demo-filled-deck.html renders 52/68/48/30/45% rings + "49% — latent". Chain documented in DEMO.md. Toolkit is dry-run-ready. Remaining human inputs: «investering»/contact tokens in 04; real client data at T–7.
